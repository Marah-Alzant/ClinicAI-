# سجل التعديلات — ClinicAI (نسخة 14 يوليو 2026)

هذا الملف يشرح **كل تعديل** تم على الكود: ما المشكلة، أين الإصلاح، لماذا بهذه الطريقة، وكيف تتحققون منه بأنفسكم. اقرؤوه كاملاً قبل العرض — المناقشون قد يسألون عن أي سطر.

> **مهم:** كل التعديلات اجتازت مجموعة اختبارات رجعية (regression) شملت: تهيئة قاعدة بيانات من الصفر، حجز أسنان روتيني كامل، حجز قلب عاجل كامل، سيناريو تعديل الوقت، سيناريو التخصص غير المتوفر، وفحص لوحة التحكم (كل الصفحات HTTP 200).

---

## الإصلاح 1: سؤال الاسم مرتين ❌→✅

**المشكلة (من تجارب الفريق):** بعد ضغط زر «حجز موعد جديد»، البوت يسأل عن الاسم، المريض يرسله، فيرد البوت بالترحيب ويسأل عن الاسم مرة ثانية.

**السبب الجذري:** المعالج (handler) كان يعيد تهيئة الـ FSM — والحالة الابتدائية هي `GREETING` — ثم يرسل سؤال الاسم بنفسه. فأول رسالة من المريض (اسمه) كانت تصل والـ FSM في حالة الترحيب، فيرد بالترحيب ويطلب الاسم مجدداً.

**الإصلاح:**
- `fsm/patient_fsm.py`: دالة جديدة `begin_booking()` تعيد التهيئة وتضع الحالة مباشرة على `COLLECT_NAME`.
- `bot/handlers/patient.py`: استدعاء `begin_booking()` في مسارَي زر القائمة (`_menu_response` و `menu:book`).
- إضافة: لو كتب المريض «اسمي فلان» في أول رسالة، يُستخرج الاسم مباشرة ولا يُسأل عنه إطلاقاً (حالة `GREETING` تتخطى لجمع الشكوى).

**كيف تتحققون:** ‎/start → «حجز موعد جديد» → أرسلوا الاسم مرة واحدة → يجب أن يسأل عن الشكوى فوراً.

---

## الإصلاح 2: عدم تطابق الهمزة في مصنّف التخصصات

**المشكلة:** المطبّع يحوّل أ/إ/آ إلى ا، لكن أنماط المصنّف كانت تحتوي همزات، فأي قاعدة فيها همزة لا تطابق أبداً. مثال حقيقي: «ألم صدر» كان يُصنَّف طب عام بدل قلب، فيضطر البوت لسؤال المريض عن التخصص يدوياً.

**الإصلاح:** `scheduler/classifier.py` — سطر واحد جوهري: تطبيع كل الأنماط عند تحميل الملف بنفس دالة `normalize` المطبقة على المدخلات.

**كيف تتحققون:** اكتبوا «عندي ألم صدر شديد» — يجب أن يُحوَّل لعيادة القلب **بدون** سؤالكم عن التخصص.

---

## الإصلاح 3: ردود Gemini الثرثارة تُحفظ كشكوى المريض

**المشكلة (ظهرت في لوحة التحكم):** عمود «الشكوى» كان يعرض ردوداً مثل «سلامتك، هاد الموضوع بيحتاج فحص سريري...» — هذا كلام البوت وليس شكوى المريض.

**السبب الجذري:** دالة `extract_missing_field` في `nlp/gemini_client.py` كانت تطلب من Gemini استخراج الشكوى، لكنه أحياناً يرد برد محادثة كامل بدل قيمة مستخرجة، وكان الرد يُخزَّن كما هو.

**الإصلاح:** في `nlp/gemini_client.py`:
1. صياغة أدق للتعليمات (بحد أقصى 8 كلمات، بدون مقدمة أو سؤال، وإلا «لا يوجد»).
2. دالة تحقق جديدة `_clean_extraction()` ترفض أي ناتج: أطول من 60 حرفاً، أو فيه علامة استفهام، أو يبدأ بعبارات بوت (سلامتك، أهلاً، بقدر أحجزلك...). عند الرفض يُستخدم كلام المريض نفسه.

**كيف تتحققون:** احجزوا بشكوى حرة ثم افتحوا لوحة التحكم — عمود الشكوى يجب أن يعرض كلمات المريض فقط.

---

## الإصلاح 4: درجة الأولوية كانت تظهر للمريض

**المشكلة:** رسالة عرض الموعد كانت تتضمن «متوسط — درجة الأولوية: 0.58». هذه بيانات فرز داخلية للعيادة، وقد تسبب قلقاً أو جدالاً مع المرضى.

**الإصلاح:** `fsm/patient_fsm.py` — حذف سطر الأولوية من رسالة العرض. الدرجة ما زالت تُحسب وتُحفظ وتظهر في لوحة التحكم كالمعتاد (لا تغيير في الخوارزمية إطلاقاً).

---

## الإصلاح 5: أزرار التأكيد تبقى بعد انتهاء الحجز

**المشكلة (ملاحظة مرح):** بعد تأكيد الحجز أو إلغائه، زرا «تأكيد الحجز/إلغاء» يظلان ظاهرين فيشك المستخدم هل الحجز تم فعلاً.

**الإصلاح:** `fsm/patient_fsm.py` — كل الردود النهائية (نجاح الحجز، الإلغاء، قائمة الانتظار، تعارض الحجوزات) تُرجع الآن **لوحة القائمة الرئيسية** بدل لا شيء، فتحل محل أزرار التأكيد وتعطي المستخدم خطوته التالية.

---

## الإصلاح 6: تعديل الموعد قبل التأكيد (طلب مريم: «بدي الساعة 10 ونص»)

**المشكلة:** في خطوة التأكيد كان البوت يقبل نعم/لا فقط. لو طلب المريض وقتاً مختلفاً يرد «اكتب نعم أو لا» ويحجز الوقت الأصلي.

**الإصلاح:**
- `fsm/patient_fsm.py`: قبل فحص نعم/لا، يفحص `_handle_confirm` وجود طلب تعديل — إما وقت صريح (أرقام، «10 ونص»، «وربع»، أسماء الساعات بالكلمات) عبر `_parse_requested_time()`، أو كلمة تعديل (عدل، بدل، وقت تاني، مش مناسب...). ترتيب الفحص مقصود: «لا، بدي وقت تاني» تعني تعديلاً لا إلغاءً.
- عند التعديل: يُستبعد الموعد المرفوض (قائمة `rejected_slot_ids`) ويُبحث عن بديل **أقرب ما يكون للساعة المطلوبة** عبر دالة جديدة في `database/crud.py` اسمها `find_alternative_slot()` — بنفس قواعد الأمان (طبيب فعّال، لا تعارض مع حجوزات المريض).
- لو ما في بديل مناسب، يُعاد عرض الموعد الأصلي بوضوح.
- **قرار تصميمي مهم للمناقشة:** لم نضف حالة (state) جديدة للـ FSM — التعديل يحدث داخل حالة `CONFIRM` نفسها، فلا تتغير خريطة الحالات ولا تنكسر أي انتقالات قائمة.

**كيف تتحققون:** احجزوا حتى تصلوا لعرض الموعد، ثم اكتبوا «بدي الساعة 10 ونص» — يجب أن يعرض موعداً عند 10:30 (أو الأقرب له) ثم أكدوا عادي. (في اختبارنا: طُلبت 10:30 وعُرضت 10:30 بالضبط وتم الحجز.)

---

## الإصلاح 7: التخصص غير المتوفر كان يسبب حلقة لا نهائية (سيناريو «عيادة صدرية»)

**المشكلة:** لما طلبت المختبِرة «عيادة صدرية» (غير موجودة عندنا)، ظل البوت يكرر نفس رسالة الأزرار بلا نهاية.

**الإصلاح:**
- `fsm/patient_fsm.py`: توسيع قاموس التخصصات ليتعرف على تخصصات قد يطلبها المرضى حتى لو غير متوفرة (صدرية، باطنية، أنف وأذن، نفسية، غدد...).
- دالة `_specialty_available()` تفحص فعلياً في قاعدة البيانات إن كان يوجد طبيب فعّال بهذا التخصص — أي أنها ديناميكية: لو أضفتم طبيب صدرية لاحقاً تشتغل تلقائياً بدون تعديل كود.
- لو التخصص غير متوفر: «عذراً، عيادة X غير متوفرة عندنا حالياً 🙏 بقدر أحجزلك عند طبيب العام ليقيّم حالتك ويحوّلك إذا لزم. يناسبك؟» — نعم → حجز طب عام، لا → «نتمنى لك يوماً سعيداً» وإنهاء مهذب (بالضبط ما طلبته مرح).
- ولو فشل الفهم مرتين متتاليتين، يُعرض نفس عرض الطبيب العام بدل تكرار الأزرار للأبد.
- `database/crud.py`: دالة جديدة `get_available_specialties()`.

---

## الإصلاح 8: ملفات البيانات المفقودة — عينة أولية

**المشكلة:** `nlp/extractor.py` يقرأ `data/levantine/symptoms.json` و `time_phrases.json` وكان المجلد غير موجود أصلاً، فمطابقة الشكاوى (العامل الأثقل وزناً في الأولوية: 0.35) كانت تعمل دائماً على قيم افتراضية.

**الإصلاح:** أُنشئ المجلد مع **عينة أولية**: 12 عرَضاً و13 عبارة وقت. ⚠️ **هذه عينة فقط — توسيعها إلى 30+ عرَضاً مهمة الفريق** (شغل بدون إنترنت، والبنية موضحة داخل الملف). ملاحظتان مهمتان للتوسعة:
1. اكتبوا الكلمات **بصيغة مطبّعة**: بدون همزات، والتاء المربوطة آخر الكلمة تصير ه (كحه لا كحة).
2. في ملف الأوقات: العبارات الأطول قبل الأقصر («بعد بكرا» قبل «بكرا») لأن المطابقة بأول إصابة.

---

## الإصلاح 9: تعارض اللحظة الأخيرة كان يعلّق المحادثة

**المشكلة (كامنة، لم تكتشفها التجارب):** لو انحجز الموعد من مريض آخر بين العرض والتأكيد، كان البوت يقول «رح أبحث عن بديل» ثم يقف في حالة ليس لها معالج — أي رسالة تالية ترد «عفواً ما فهمت».

**الإصلاح:** `fsm/patient_fsm.py` — يبحث عن البديل **فوراً** ويعرضه في نفس الرد.

---

## تحسينات النظافة (Hygiene)

| البند | ماذا تغيّر |
|---|---|
| `requirements.txt` | كان بترميز UTF-16 (يكسر pip على لينكس/ماك) — أعيد حفظه UTF-8 |
| `.gitignore` | أُضيف `*.db` و `.env` |
| `.env.example` | نسخة قالب — انسخوها إلى `.env` وضعوا التوكنات الحقيقية (لا ترفعوا `.env` أبداً بعد الآن) |
| `database/clinic.db` | حُذف من النسخة — يُبنى تلقائياً عند أول تشغيل مع 8 أطباء و960 موعداً |
| `__pycache__` | حُذفت المجلدات |
| `README.md` | جديد — تعليمات التشغيل كاملة |

---

## ما الذي لم يتغيّر (مهم للمناقشة)

- **خوارزمية الأولوية**: نفس العوامل الخمسة ونفس الأوزان والعتبات — لم تُمس.
- **بنية الـ FSM**: نفس الحالات والانتقالات، لا حالات جديدة.
- **مخطط قاعدة البيانات**: لا تغيير في الجداول أو الأعمدة.
- **واجهة لوحة التحكم**: كما هي (أُعيد اختبارها: كل الصفحات تعمل).

## المتبقي على الفريق

1. توسيع `symptoms.json` إلى 30+ عرَضاً (الأولوية الأولى — بدون إنترنت).
2. بناء مجموعة بيانات التقييم الموسومة للفصلين 4 و5 (انظروا تقرير المراجعة، القسم 5).
3. التمرن على سيناريوهات العرض الثلاثة قبل الخميس.
